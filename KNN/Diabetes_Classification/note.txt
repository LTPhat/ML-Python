https://www.mygreatlearning.com/blog/gridsearchcv/
Preprocessing:
  data.fillna(data.means/median)
  data.info(verbose=True)
  data.iloc(index,index)
  data.replace({old:new}
  data.isnull().sum(): Check NaN value
  pd.plotting.scatter_matrix
  sns.pairplot(data,hue)
  sns.heatmap(data, annot = True)
  StandardScaler()
Modeling
  X_train,X_test,y_train,y_test = train_test_split(X,y,test_size = 1/3,random_state = 42, stratify = y)
  knn = KNeighborsClassifier(n_neighbors = k,weights ='uniform').fit(X_train,y_train)
  sns.lineplot()
Evaluation
  confusion matrix
  ROC curve
  Precision-Recall
HYPERPARAMETER with GridSearchCV
